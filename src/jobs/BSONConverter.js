define('src/jobs/BSONConverter', [], function() {

    function BSONConverter(options) {
        this.options = options;
    }

    BSONConverter.prototype = {
        execute: function(parameters, completion) {

        }
    }

    return BSONConverter;
});